<div class="task">
    <h4>${ task.summery }</h4>
    <div>${ task.details }</div>
    <div class="due">Due Date : "${ task.deadline }"</div>
    <div class="Edit">  <input type="checkbox" name="taskid" value=${ task.id }> Check to mark as complete/Delete</div>
    <div class="created">Created On "${ task.dateCreated }"</div>
    
</div>
